sudo apt install git
 git --version
git config --global user.name priyanka
git config --global user.email elavandepriyankagmail.com
git initmyrepo  (myrepo folder can be created into file manager
cd myrepo (change directory)
list -a (display hide file)
touch sample.txt(first create new text file into folder and write anything in that file)
git add sample.txt(add all file data)
git add.
git status
git commit -m "first change"
git log(check history)
git branch "branch1"(create new branch)
git checkout "branch1"(checkout onto new branch)
touch newfile.txt(create new file into folder)
git add.
git commit -m "second change"
git status
git checkout master
git merge branch1

